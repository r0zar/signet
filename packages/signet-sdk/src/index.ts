/**
 * Signet SDK - Lightweight messaging API for Signet extension communication
 */

// Export the core messaging system
export { send, request, respond, subscribe } from './messaging';
export type { Message } from './messaging';